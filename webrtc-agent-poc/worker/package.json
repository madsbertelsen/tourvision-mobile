{
  "name": "webrtc-agent-poc-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker signaling server for WebRTC agent POC",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8788",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.90.0",
    "typescript": "^5.7.2"
  }
}
